[package]
name = "tvp-kagparser"
description = "KAGParser native class in Rust: loads .ks scenarios and steps through parsed tags (KAG engine behind ADV text scenes)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tjs2-sys = { workspace = true }
engine = { path = "../engine" }
kag = { path = "../kag" }
tvp-util = { path = "../tvp-util" }
log = "0.4"
