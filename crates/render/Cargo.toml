[package]
name = "render"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Bevy render crate: turns the tvp-visual Scene into rendered Bevy entities"

[lib]
name = "krkr_render"

[[bin]]
name = "krkr-vn"
path = "src/main.rs"

[dependencies]
bevy = "0.19"
engine = { path = "../engine" }
log = "0.4"
tjs2-sys = { workspace = true }
tvp-kagparser = { path = "../tvp-kagparser" }
tvp-natives = { path = "../tvp-natives" }
tvp-scripts = { path = "../tvp-scripts" }
tvp-storages = { path = "../tvp-storages" }
tvp-visual = { path = "../tvp-visual" }
