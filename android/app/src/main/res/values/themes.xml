<?xml version="1.0" encoding="utf-8"?>
<!--
  Platform themes. The UI is drawn by Compose (Material 3), so these only need
  to provide a sane window background, no action bar, and an edge-to-edge
  fullscreen variant for the engine Activity. They intentionally avoid the
  Material Components XML themes so the app needs no extra dependency.
-->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Krkr" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/background_light</item>
    </style>

    <style name="Theme.Krkr.Fullscreen" parent="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
    </style>
</resources>
